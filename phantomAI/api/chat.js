import OpenAI from "openai";
const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});
export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{const{messages}=req.body||{};if(!Array.isArray(messages)||!messages.length)return res.status(400).json({error:"No messages supplied."});const safeMessages=messages.filter(m=>m&&(m.role==="user"||m.role==="assistant")&&typeof m.content==="string").slice(-30);const response=await client.responses.create({model:"gpt-5.5",instructions:"You are PhantomAI, a helpful, accurate AI assistant created by Phantom Security. Answer clearly. For coding questions, provide practical code and explain important steps. Do not claim to have performed actions you did not perform.",input:safeMessages.map(m=>({role:m.role,content:m.content}))});return res.status(200).json({text:response.output_text||""})}catch(error){console.error(error);return res.status(500).json({error:"AI request failed. Check your OPENAI_API_KEY and deployment logs."})}}
